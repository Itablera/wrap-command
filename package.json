{
  "name": "@itablera/wrap-command",
  "version": "0.7.21",
  "description": "Wrapper for command framework",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "repository": {
    "url": "https://github.com/Itablera/wrap-command.git"
  },
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@itablera/wrap-common": "^0.7.36",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.3.3",
    "rimraf": "^5.0.1",
    "typescript": "~5.1.6"
  }
}
