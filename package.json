{
  "name": "@itablera/wrap-command",
  "version": "0.7.21",
  "description": "Collection of utilities and common domains",
  "main": "src/index",
  "types": "src/index",
  "files": [
    "src"
  ],
  "repository": {
    "url": "https://github.com/Itablera/wrap-command.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@itablera/wrap-common": "git+ssh://git@github.com:Itablera/wrap-common.git#main",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.3",
    "rimraf": "^3.0.2",
    "typescript": "~4.7.0"
  }
}
